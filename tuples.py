#tuples are like dictionaries and list only that they are not modifiable once defined
tuple_example = ("fred", "williams", "charles")
print(tuple_example[1])

tuple_example_1 = (23,19,12)
print(max(tuple_example_1))

# double tupling
(length, height) = (16,14)
print(length)